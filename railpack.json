{
  "build": {
    "cmd": "composer update --no-dev --optimize-autoloader && php artisan migrate --force && php artisan db:seed --force && php artisan storage:link"
  },
  "start": {
    "cmd": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
